doctype
html
  head
    title Ranking tagpro
    meta(charset='utf-8')
    link(href='/imgs/red_flag.png', rel='icon', type='image/x-icon')
    link(rel='stylesheet', type='text/css', href='/styles.css')
    script(type='text/javascript', src='/jquery-3.1.1.min.js')
  body
    a.button(style='clear:both;float:left;margin-top:20px;margin-left:10px;margin-bottom:20px;', href='/')
      | Accueil
    h2(id='playerName' style='clear:both;margin-top:0px')
    h3(id='mmr' style='clear:both;margin-top:0px')
    script(type='text/javascript').
      var myData = !{JSON.stringify(matchs_table)};
      $('#playerName').text(myData.team1);
      $('#mmr').text('Score actuel: '+myData.added_by)
      // Simple asynchronous requests to server
      function xhr(method, url, data, callback, errorCB) {
      var req = new XMLHttpRequest()
      req.open(method, url, true);
      req.onload = function(e) {
      if (this.status.toString()[0] === '2') {
      callback(JSON.parse(this.responseText));
      } else {
      errorCB(this.status, this.responseText);
      }
      }
      if (method === 'POST') {
      req.setRequestHeader('Content-Type', 'application/json;charset=UTF-8');
      req.send(JSON.stringify(data));
      } else {
      req.send();
      }
      }


      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-81626852-1', 'auto');
      ga('send', 'pageview');
